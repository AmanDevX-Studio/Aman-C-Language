#include <stdio.h>

int main()
{
    int a, b, mullti;
    system("cls");
        printf("Enter Two Number : ");
    scanf("%d %d", &a, &b);

    mullti = a * b;
    printf("Multiplication of two number : %d\n", mullti);
    return 0;
}