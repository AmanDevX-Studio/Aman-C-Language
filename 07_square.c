#include<stdio.h>
#include<math.h>

int main(){
    //squaring of two numbers
    int a, squar;
    printf("Enter the number : ");
    scanf("%d",&a);
    squar = sqrt(a);
    printf("Square is : %d\n",squar);
    return 0;
}